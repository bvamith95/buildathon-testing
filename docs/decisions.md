# Una — Decision log

Resolves the open decisions in `docs/prd.md` and the blocking/important items raised in `docs/prd-review.md` and `docs/architecture.md`. Decided 2026-09-20 with the product owner. Each entry below is now the authoritative answer; `prd.md`, `prd-review.md`, and `architecture.md` are annotated to point here rather than restate it.

## From the PRD's open decisions

| # | Decision | Resolution |
| --- | --- | --- |
| 1 | Confirm the demo date | Kept at 16 October 2026. Build starts today, 20 September 2026 — about 3.7 weeks, roughly 2 days tighter than the PRD's nominal 4-week plan. Proceed on the existing week-by-week plan; watch week 4 for slack. |
| 2 | Approve the pre-generated architecture | Approved as written: nightly offline RAG across 12 variants, human review gate, sub-second cache-read serving path. Not revisited for this build; live generation stays a post-MVP option once traffic/freshness needs are better understood. |
| 3 | Name the review owner | Product owner (you) is the review owner — signs off changed steps before each publish, and is the one who gets crawl-failure alerts (see below). |
| 4 | Freeze the bucket taxonomy | Five buckets stand (entry document, biometrics, medical exam, funds evidence, currency corridor). Coverage policy below (§ Unmatched bucket signature) governs what happens when a real signature falls outside the initially generated set — it does not reopen the taxonomy itself. |
| 5 | Decide the product name | **Resolved 2026-09-24.** The product is **Una**, tagline "una for uni" for now. See item 13 below for the accompanying logo/color work. |
| 6 | Set the recheck window | 30 days for immigration content, 90 days for everything else, as the PRD suggested. |
| 7 | Confirm the metric denominators | Confirmed as proposed: completion rate denominator is the program-level tap (not landing page view); helpful rate excludes users who rate individual steps but never give an overall rating (tracked separately, not folded in as neutral). |
| 8 | Decide on a not-applicable state | Adding it. Steps get a third state — done / not done / not applicable — specifically to give the review owner a signal when an `applies_to_rules` tag is wrong for a bucket. |
| 9 | Cap the step count | 20 steps, matching the PRD's own stated ceiling for phone navigability (up from today's 16). |
| 10 | Agree who sends reminder emails | Product owner (you) owns securing the sending domain and unsubscribe mechanism. If not ready by week 3, the PRD's existing fallback applies: cut the feature for this build. |

## From the architecture review

| # | Item | Resolution |
| --- | --- | --- |
| 1 | Confidence threshold mechanism | Baseline is embedding similarity between a step's claim and its best-matching retrieved chunk. The review owner tunes the per-bucket cutoff after week 1's single end-to-end bucket pilot, rather than shipping one global number on day one. |
| 2 | Review-gate rejection behavior | Per-step revert: a rejected step reverts to its last-published content; every other approved step in that variant still publishes under the new `content_version`. Confirms the mechanism already assumed in `architecture.md` §3. |
| 3 | Bucket signature with no pre-generated variant | Treated as a taxonomy signal, not a serve-time edge case: an observed real signature outside the initial ~6 gets added to the generation set (through the normal generate → diff → review → publish pipeline), rather than permanently routed to a "closest match" or "not covered" state. **Open follow-up:** the interim behavior for a user who hits an unmatched signature *before* it's been added (i.e., during the gap between first observation and the next reviewed publish) is not yet decided — needs a follow-up call before week 2, since the online serving path (architecture.md §4–5) still needs some rendered response in that gap. |
| 4 | Crawl-failure alerting | After 3 consecutive failed crawls on the same source, the pipeline alerts the review owner alongside the nightly review queue, rather than relying on the stale banner alone to surface it. |
| 5 | Undergraduate path disclosure | A light in-product caveat ships on the undergraduate guide (e.g. "this path is newer and less tested than the graduate path"), matching the same honesty standard already applied to source last-checked dates. Internal-only framing at the demo (per the PRD's risk table) stays in addition to this, not instead of it. |
| 6 | `profile_hash` re-identification risk | `profile_hash` is salted per anonymous `session_id` before being sent in feedback events, so it can't be matched across sessions or against the plain profile parameters in a shared URL. `content_version`-based regression analysis still works within a session. |
| 7 | Housing pointer links | Two links: the UBC housing website and the UBC Facebook roommates group. No affiliation is claimed — the landing page states plainly that Una doesn't cover housing and these are just starting points. The product owner verifies and updates these two links directly (not through the whitelist/citation pipeline, since housing content isn't whitelisted). A full housing feature is acknowledged as a likely future addition given the demand and anxiety the survey surfaced, but is explicitly not in scope for this build. |
| 8 | Sample-size caveat visibility | No change. The PRD's existing inline caveats (graduate-only sample, warm demo panel, all-arrived-successfully) are sufficient; no additional top-of-document disclaimer. |

## Resolved 2026-09-24

| # | Item | Resolution |
| --- | --- | --- |
| 9 | Whitelist page list | Product owner signed off on the 22 researched URLs in `pipeline/landfall_pipeline/crawler/whitelist.py` (7 IRCC, 7 UBC, 3 Government of BC, 3 Service Canada, 2 CRA), unblocking the Week 1 pilot run pending only the Anthropic billing and Voyage network-access items already tracked in Linear (SAA-66). Liveness wasn't verified from this sandbox (network policy blocks all four domains) — re-check reachability the first time the pipeline actually runs somewhere with real network access. |
| 10 | "CRA" terminology | Corrected. The fourth whitelist org was mislabeled "CRA" throughout `prd.md`/`architecture.md`/`CLAUDE.md` — the SIN-application pages are actually published by Service Canada (ESDC), not the Canada Revenue Agency. Docs now say "Service Canada/CRA" for the whitelist-boundary label; each whitelist entry is individually tagged with its true publisher so per-page citations shown to users stay accurate regardless. |
| 11 | Confidence threshold, tuned from real data (Linear SAA-72) | `DEFAULT_CONFIDENCE_THRESHOLD` lowered from the 0.75 placeholder to 0.5. The Week 1 pilot's first pass sent all 8 steps to `no_source` despite retrieval finding the correct page every time — actual correct-match scores were 0.56–0.67. This was the pilot's forcing function working exactly as designed (see the PRD review's original blocking item on this threshold). Calibrated against IRCC/UBC/BC/Service Canada content specifically; re-tune per bucket as more pilot data comes in. |
| 12 | Week 1 pilot run complete (Linear SAA-15) | Bucket: graduate, India-shaped (visa required, biometrics required, medical exam required) — the same profile the PRD names as its Week 2 validation target. 16/22 whitelisted pages crawled, 136 chunks, 8 steps generated across all 5 PRD phases, 7 verified + 1 correctly withheld as `no_source` (a real coverage gap from the `students.ubc.ca` crawl block, Linear SAA-73 — not a threshold problem). Product owner reviewed and approved all 8; published as `apps/web/public/guides/visa_required-required-required-country_programme_variant-restricted__graduate.json`, `content_version=1`. This satisfies the PRD's Week 1 "done means." |
| 13 | Product name + partial brand identity | Name decided: **Una**, tagline "una for uni" for now (item 5 above). Product owner provided a wordmark and icon logo (lime-green, `apps/web/public/logo/`). Primary brand color sampled directly from the logo files: `#C6D531`, which needs dark (not white) foreground text for contrast — verified at ~13:1 against near-black vs ~1.6:1 against white. Applied as the accent color across the Landing screen (primary CTA, bullet markers) and as the site's favicon/header mark. Typography and the rest of the design system are explicitly not decided yet — product owner said to revisit later (SAA-63 stays open for that). |
| 14 | Interim behavior for an unmatched bucket signature (Linear SAA-22) | Serve the nearest existing published guide for the same `program_level`, with a visible "your exact situation isn't covered yet — showing the closest match" banner. Never render nothing. Nearest-match is a weighted comparison across the 5 bucket dimensions (`entry_document` > `biometrics` > `medical_exam` > `funds_evidence` > `currency_corridor`, weights 16/8/4/2/1) — earlier dimensions matter more because they gate whether whole steps exist, not just wording inside them. A client-side lookup among already-published static guides, no backend call. Full algorithm in `architecture.md`, "Decisions resolved 2026-09-24" item 5. This was the repo's last open decision — none remain as of this entry. |
| 15 | Bucket taxonomy freeze — the "roughly six realistic combinations" (Linear SAA-9, feeds SAA-15/SAA-23) | Frozen at **7** signatures, not 6 — one more than the PRD's rough estimate, because that's what the real IRCC eTA/biometrics/medical-exam research (done for the Intake screen's citizenship table) actually produces across 21 representative citizenships incl. UBC's top source countries. Trimming to exactly 6 would have meant merging Iran into the pilot bucket and losing its one real distinction (no medical exam required). List: (1) visa_required/required/required/country_programme_variant/restricted — China, India, Nigeria, Vietnam, Bangladesh, Pakistan, Philippines, Indonesia; already published as the Week 1 pilot. (2) eta/exempt/not_required/standard/major — USA. (3) visa_required/required/not_required/country_programme_variant/restricted — Iran. (4) eta/required/required/standard/major — South Korea, Hong Kong. (5) eta/required/not_required/standard/major — France, Japan, UK, Germany, Taiwan, Australia. (6) visa_required/required/not_required/standard/major — Mexico, Saudi Arabia. (7) visa_required/required/required/standard/major — Brazil. Encoded in `pipeline/landfall_pipeline/generation/buckets.py`'s `REALISTIC_COMBINATIONS`, kept in sync with the client-side table in `apps/web/src/lib/buckets.ts`. |
| 16 | Week 2 bucket rollout process | Which steps exist per bucket, and what each covers, is derived mechanically from the PRD's bucket table (`pipeline/landfall_pipeline/generation/step_plans.py`) rather than hand-written per bucket — e.g. a biometrics-exempt bucket has no biometrics step at all, rather than an empty one. Product owner reviewed one checkpoint bucket's actual generated content by hand (`eta-exempt-not_required-standard-major`, graduate — the USA bucket) before authorizing the remaining 12 (bucket, level) combinations to generate and publish unattended via `pipeline/run_remaining_buckets.py`, on the basis that every step still carries the same no-citation-no-claim guarantee the checkpoint demonstrated. All 14 (bucket, level) combinations from the frozen taxonomy are now published, satisfying the PRD's Week 2 "done means" (Linear SAA-25). |
| 17 | SAA-73 verified fixed; `apply-for-sin`'s real root cause | The `students.ubc.ca` User-Agent fix (item in the Week 1 pilot record, SAA-73) was committed but never actually re-run — the cached retrieval index still had zero `students.ubc.ca` pages when Week 2's bucket rollout started, so every graduate bucket's `apply-for-sin` step (7 of them) landed `no_source` again. Re-crawling confirmed the fix works: all 6 previously-blocked UBC pages, including the one that specifically covers SIN-for-TAs/RAs (`students.ubc.ca/finances/taxes/social-insurance-number-sin/`), now return 200s. Added `pipeline/refresh_pages.py` to merge newly-crawlable pages into the cached index without re-touching already-cached sources on the flaky `www.canada.ca` domain, and `pipeline/patch_step.py` to regenerate one step across many published guides without a full bucket re-run. All 7 graduate guides' `apply-for-sin` step is now `verified` at 0.61 confidence, citing the correct UBC page, `content_version=2`. Also fixed in passing: `StepGenerator` was allowed to cite the same URL twice in a step's 2-source list when its top-2 retrieved chunks came from the same page (`generator.py` now dedupes by URL) — applies to generation from now on; not worth the API cost to retroactively regenerate already-published steps for this cosmetic-only fix. |
| 18 | Overall rating prompt: floating action button, not an inline banner (Linear SAA-43) | Product owner feedback 2026-09-25: the prompt's trigger is "first deep scroll," which by definition means the user has already scrolled past the top of the page by the time it fires — an inline banner inserted at the top of the Timeline appears behind them, exactly where they won't see it. Changed to a `position: fixed` floating button, bottom-right, visible regardless of scroll position, tapping it opens a small popover with the same message + thumbs. Same component on mobile and desktop rather than a separate sticky-header variant for mobile: a sticky header would permanently cost vertical space on an already-small screen, while the floating button costs nothing until tapped, and a bottom corner sits in the natural one-handed thumb zone. Mobile safe-area aware (`env(safe-area-inset-bottom)`) so it doesn't collide with a phone's home-indicator/gesture-bar zone. If a future feature (reminder opt-in, share) wants a similar floating corner affordance, coordinate placement with this one rather than overlapping the same corner — noted in code (`apps/web/src/app/guide/page.tsx`'s `FloatingRatingButton`) but no shared stacking system built yet, since only one floating element exists today. |
| 19 | First-phase label: "After you accept your offer", not "After your offer" | Product owner feedback 2026-09-25: "After your offer" read as unclear/incomplete on its own. Changed the displayed phase label (not the internal `after_your_offer` phase key, which stays as-is across `step_plans.py`, `run_pilot.py`, and every published guide JSON's `phase` field — those are stable content-model identifiers, not user-facing copy) in `apps/web/src/lib/guide.ts`'s `PHASES` array and the matching row in `docs/prd.md`'s phase table. Note for future copy changes to this label: `docs/prd.md` states the phase wording was chosen to mirror the original student survey's own stage wording — this change trades a slightly closer echo of that source language for clarity, which the product owner judged worth it. |

## Still open

None currently.
